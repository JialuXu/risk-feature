# risk_rule_pruning 业务需求文档（PRD）

> 文档类型：用户视角的功能需求说明
> 适用范围：拟新增子 Skill `risk_rule_pruning/`（规则集优化 / 规则优化建议清单）
> 文档版本：v0.2（2026-06-08，草案，待评审）
> 最终交付物：**一份规则优化建议清单**——逐条说明哪条规则要优化、怎么优化、影响多大；前沿曲线是支撑手段。
> 关联文档：[PRD-risk-feature-pipeline.md](PRD-risk-feature-pipeline.md) · [GLOSSARY.md](GLOSSARY.md)

---

## 1. 总体定位

### 1.1 业务背景

风险预警规则上线后，运营团队普遍遇到一类"规则越加越多"的困境：

1. **触碰率失控**：报告里每发现一个有区分度的特征，就新增一条预警规则。规则只增不减，结果是**被预警的客户越来越多**，一线核查人力跟不上，预警逐渐沦为"狼来了"。
2. **不知道哪条该砍**：明知很多规则是冗余的（它命中的坏客户，别的规则也都命中了），但没有量化办法说清"砍掉这条，召回到底掉不掉"，于是谁也不敢动，规则只能一直留着。
3. **说不清"留多少够用"**：管理层问"我们这套规则，能不能少一半还保住九成坏客户?"——目前回答不了，因为缺少一条把"触碰多少客户"和"抓住多少坏客户"摆在一起看的曲线。

现有链路已经能产出**客户级命中矩阵**（`risk_trigger_extraction`，每行一个客户、每条规则一列、命中填 1，并已知谁是坏客户），但它只评估"规则全开"这一个点，没有回答"往回砍"的问题。

### 1.2 产品目标

`risk_rule_pruning` 是一个**规则集优化决策支持工具**。它的**最终交付物是一份"规则优化建议清单"**——逐条告诉运营团队：

> **哪些规则需要优化、每条该怎么优化（删除 / 收紧阈值 / 合并 / 降级观察 / 保留）、为什么、对触碰率和召回的影响有多大。**

前沿曲线、贪心反向剔除是**得出这份清单的分析手段，不是终点**。围绕这个最终目标，工具要回答：

- **每条规则的"体检结果"是什么?** —— 它独占抓住多少坏客户、命中客户里坏率多高、和哪条规则高度重叠、性价比排第几。
- **据此每条规则该做什么动作?** —— 给出明确的优化动作 + 理由 + 量化影响（动作类型见 §2.3）。
- **整体砍到什么程度合适?** —— 用前沿曲线 + 拐点给出"建议保留哪些规则"的整体收口位置。

核心理念：**不发现新规则、不动标签**，只对现有规则集合给出"删/改/留"的优化建议；其中"收紧阈值"类建议交接给 `risk_threshold_explore` 落地，本模块只负责诊断并指出方向。所有数字从矩阵现算、可复盘。

### 1.3 用户角色

| 角色 | 关心什么 | 主要使用方式 |
|---|---|---|
| **审批策略产品 / 规则运营** | 拿到"哪条规则要删/要改/要留"的可执行清单 | 看规则优化建议清单，逐条落地 |
| **风险分析师** | 给出量化依据、可复盘、不黑盒 | 跑建议清单 + 前沿表，向上汇报 |
| **风险管理负责人** | "能不能少一半规则保住九成坏客户" 这类决策 | 看建议清单汇总 + 一张前沿曲线图拍板 |
| **AI Agent**（如 Claude Code） | 状态可读、结论可机器消费 | 子命令 + audit 节点 |

---

## 2. 业务场景与价值

### 2.1 典型场景

> 某行对公预警有 28 条规则全开，月度触碰 4200 户，核查团队只能覆盖 1500 户。分析师跑一次规则优化，拿到**建议清单**：6 条标"删除（完全冗余）"、3 条标"合并（与其它规则高度重叠）"、4 条标"收紧阈值（触碰巨大但命中坏率极低）"、其余"保留"。照清单执行后，触碰从 4200 降到 1800 户、召回从 94% 仅降到 93%，人力刚好覆盖。每条建议都附了理由和影响数字，能直接进规则评审会。

### 2.2 业务价值

- **可直接执行**：交付的不是一条曲线，而是"这 6 条删、这 3 条合并、这 4 条收紧阈值"的动作清单，运营拿来就能落地。
- **省人力**：把"无效触碰"量化出来并安全砍掉，核查资源投到真有风险的客户上。
- **保召回**：每条优化动作都标出召回代价，避免"砍过头漏掉坏客户"。
- **可解释、可复盘**：每条规则为什么给这个建议、影响多少，全部留痕，能向规则评审会和审计交代。

### 2.3 优化动作清单（最终交付物的核心）

每条规则会被归到下列动作之一，并附**判定理由 + 量化影响**。判定信号全部从命中矩阵现算（定义见 §3）：

| 优化动作 | 触发情形（业务语言） | 给运营的话术 | 影响口径 |
|---|---|---|---|
| **删除（冗余）** | 它命中的坏客户，别的规则全都命中——独占坏客户 = 0 | "删了召回一点不掉，纯减负" | 省的触碰、召回变化（应为 0） |
| **合并** | 与某条规则在坏客户上高度重叠（如重叠 ≥ 阈值） | "和 X 规则几乎重复，建议并成一条减维护" | 重叠规则、重叠度 |
| **收紧阈值** | 触碰量很大但命中客户里坏率极低——抓得多但抓得脏 | "别删，触碰太宽，交 threshold_explore 重定阈值" | 命中坏率、触碰贡献、转交对象 |
| **降级观察** | 性价比差、在拐点前被剔，删了有小幅召回损失 | "降为观察名单而非硬预警" | 省的触碰、丢的召回 |
| **保留（核心）** | 独占坏客户多 / 命中坏率高，是召回的顶梁柱 | "不要动" | 独占召回、命中坏率 |

> "收紧阈值"是**指方向、交接**给 `risk_threshold_explore`，本模块不亲自改阈值；"合并"为建议，不自动改线上规则。动作的判定门槛（重叠度、坏率、性价比分位等）默认值可配，见 §8。

---

## 3. 业务需求（功能性）

> 用业务语言描述"要能做到什么"，不约束实现。BR-7 是最终目标，BR-1~BR-6 是支撑它的手段。

### BR-1 读取命中矩阵与坏客户标签
系统要能拿到一份"行=客户、列=每条规则（命中=1/未命中=0）、并标明每个客户是否为坏客户"的数据。优先复用 `risk_trigger_extraction` 已产出的命中矩阵；矩阵里若缺坏客户标签，系统要能把标签补齐后再算。

### BR-2 计算任意规则组合的两个核心指标
对任意一组规则，系统要能算出：
- **触碰率** = 被这组规则（任一条命中即算）预警的客户数 ÷ 总客户数；
- **召回率** = 被预警的坏客户数 ÷ 全部坏客户数。
> 业务含义：触碰率代表"要核查多少户/人力成本"，召回率代表"抓住了多少坏客户/风险覆盖"。

### BR-3 给出"全开"基线
系统要先算出"所有规则全开"这一现状点的触碰率与召回率，作为对照基准——这就是团队当前所处的位置。

### BR-4 给每条规则做"体检"（单规则诊断）
对每条规则，系统要能算出并给出下列体检指标，作为 §2.3 优化动作的判定依据：
- **独占坏客户数 / 独占召回**：只有这条规则抓住、其它规则都没抓住的坏客户——衡量"删了会丢多少召回"。
- **独占触碰数**：只被这条规则预警的客户——衡量"删了能省多少人力"。
- **命中坏率**：这条规则命中的客户里坏客户占比——衡量"抓得准不准"（高=精准，低=抓得脏）。
- **召回贡献 / 触碰贡献**：单看这条规则抓住的坏客户占总坏客户、命中客户占总客户的比例。
- **最大重叠规则 + 重叠度**：在坏客户上与它重合最多的那条规则及重合程度——支撑"合并"判定。

### BR-5 贪心反向剔除并生成前沿曲线（手段）
系统按"性价比最差先砍"的原则一条条剔除规则（性价比 = 每省一个触碰要付出多少召回代价），每砍一步记录一个（触碰率，召回率）点，直到召回跌到**用户设定的底线**（默认 90%）为止。这些点连成**触碰率-召回前沿曲线**，并记录每条规则被剔的步序与代价——它既支撑"降级观察"判定，也给出整体收口位置。

### BR-6 标注建议运营点（整体收口）
系统要在前沿曲线上自动识别"召回开始明显受伤"的拐点，标为**建议运营点**，给出该点对应的：保留哪些规则、触碰率、召回率。这是整体"砍到哪"的建议、不是强制。

### BR-7 ⭐ 产出规则优化建议清单（最终交付物）
系统要把 BR-4 的体检 + BR-5/BR-6 的剔除结果，**汇总成一份逐规则的优化建议清单**，每条规则一行，至少包含：
- **规则标识**（业务可读名称）；
- **优化动作**：删除 / 合并 / 收紧阈值 / 降级观察 / 保留（取值见 §2.3）；
- **判定理由**：为什么给这个动作（如"独占坏客户=0，完全被 X、Y 覆盖"）；
- **量化影响**：照此动作执行，触碰率、召回率各变动多少；
- **交接对象**（如适用）：如"收紧阈值 → risk_threshold_explore"。
> 这份清单是给规则评审会的，要能直接读、直接拍板、直接派活。

### BR-8 召回底线与动作门槛可配
召回底线默认 90%，用户可按业务要求调整（如监管口径要求 95%）。§2.3 各动作的判定门槛（重叠度、命中坏率、性价比分位等）也应有默认值且可覆盖。

---

## 4. 业务规则与约束

- **绝不手改标签**：召回的分子分母全部从矩阵 + 坏客户标签现算，任何环节不得人工修改谁是坏客户。
- **冗余规则优先砍**：完全被其他规则覆盖（砍了不丢任何召回）的规则，应在最前面被剔除——这对应曲线起始的"平段"。
- **代价透明**：每砍一步都必须能说清省了多少触碰、丢了多少召回，不允许"打包砍掉一批说不清账"。
- **建议不替代决策**：拐点/建议运营点只做标注与提示，不自动落地为线上规则变更。
- **方法局限须写明**：贪心是逐步最优的经验前沿，不保证全局最优，须在自检记录里声明，避免被当成"数学最优解"。
- **不推进成熟度等级**：本模块是 Level 1（已导出结果）之后的只读后置分析，与查询/可视化同级，**不改变链路状态**。
- **合规**：中文输出；任何产物不出现客户名称、客户号、电话等可识别信息；导出 CSV 用带 BOM 的 UTF-8。

---

## 5. 输入 / 输出（业务视角）

### 5.1 输入
| 项 | 业务含义 | 来源 |
|---|---|---|
| 命中矩阵 | 谁命中了哪几条规则 | `risk_trigger_extraction` 的客户级宽表（首选） |
| 坏客户标签 | 谁是坏客户（逾期/不良） | 同矩阵自带，或回查准备好的宽表补齐 |
| 召回底线 | 最低能接受的坏客户覆盖率 | 用户设定，默认 90% |

### 5.2 输出
| 产物 | 给谁看 | 解决什么 |
|---|---|---|
| **⭐ 规则优化建议清单** | 规则评审会 / 运营 | **最终交付物**：每条规则删/改/留 + 理由 + 影响，拿来即执行 |
| 单规则体检表 | 分析师 | 每条规则的独占召回、命中坏率、重叠规则等判定依据 |
| 前沿曲线表 + 曲线图 | 分析师 / 管理层 | 整体"砍到哪"的人力-召回权衡，标平段/拐点/底线 |
| 建议运营点 | 决策人 | 整体收口的有依据数字 |
| 自检记录 | 审计 / AI Agent | 用了什么数据、什么口径、各动作门槛、方法局限 |

---

## 6. 验收标准

1. **建议清单完整可执行**：每条规则都有一个明确动作（删除/合并/收紧阈值/降级观察/保留）+ 理由 + 量化影响，无"未分类"规则。
2. **动作判定正确**：构造样例——完全冗余规则必须判"删除"；与某规则高度重叠的判"合并"；高触碰低坏率的判"收紧阈值"；独占召回高的判"保留"。
3. **影响数字自洽**：照清单把所有"删除"动作执行后的实际触碰率/召回率，与清单里累计预估的影响一致。
4. **基线正确**：全开点的触碰率、召回率与 `risk_trigger_extraction` 现状一致（同一份矩阵交叉核对）。
5. **单调性**：随着规则被一条条砍掉，触碰率单调不增、召回率单调不增（不出现砍了规则召回反升的异常）。
6. **底线生效**：召回跌破设定底线即停止；拐点落在"平段+陡降"样例的拐点附近而非两端。
7. **可复盘**：自检记录能说清数据来源、坏客户口径、召回底线、各动作门槛、方法局限。
8. **合规**：产物无任何客户可识别信息，CSV 可被 Excel 正确打开（中文不乱码）。

---

## 7. 不在本期范围（Out of Scope）

- **发现新规则 / 新特征** —— 属 `risk_rule_mining` / `risk_threshold_explore`。
- **亲自调整单条规则的阈值** —— 本模块只会在建议清单里指出"这条该收紧阈值"并交接给 `risk_threshold_explore`，不亲自计算/改写新阈值。
- **亲自执行合并规则** —— 只给"建议合并 X 与 Y"，不自动生成合并后的新规则。
- **正向贪心（从零开始逐条加规则）** —— 本期只做反向剔除，正向作为后续可选增强。
- **自动把建议运营点落地为线上规则变更** —— 本模块只给建议，不改线上系统。
- **客户-月口径的滚动监控** —— 本期以客户级快照为主，客户-月口径作为后续扩展（届时需明确去重与分母口径）。

---

## 8. 待业务确认

1. **召回底线默认值**：90% 是否合适，还是按你们监管/内控口径定（如 95%）?
2. **拐点判定口径**：用"召回随触碰下降斜率突变"自动识别，还是用固定规则"每多砍 1% 触碰、召回掉超过 X% 就停"? 倾向前者，是否需要可配?
3. **坏客户口径**：以现有 `is_bad`（逾期=1）为准，是否需要支持多档（如关注/次级/可疑/损失）分别跑?
4. **运营点是否需要多个**：除拐点外，是否要同时给"召回≥90%/≥95% 的最低触碰点"等几个候选运营点供选?
5. **动作门槛默认值**（影响每条规则被判成什么动作，请拍板或先用建议默认）：
   - "合并"的重叠度门槛：坏客户重合 ≥ 多少算高度重叠?（建议默认 0.9）
   - "收紧阈值"的命中坏率门槛：命中客户里坏率低于多少算"抓得脏"?（建议参照全样本坏率的某倍数）
   - "降级观察" vs "删除"：拐点前被剔且召回损失小于多少算可降级而非硬删?
6. **建议清单要不要给"动作优先级/工作量排序"**：比如先做收益最大的删除、再做合并，方便运营排期。
